extends layout

block content
  h1= title
  p Welcome to the #{title}

  if error
    p Error getting dynamic content.
  else
    p The database has the following record counts:

    ul
      li #[strong Forecasts:] !{data.forecast_count}
